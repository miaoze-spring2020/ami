version: 2
jobs:
  build:
    docker:
      - image: hashicorp/packer:1.1.1

    steps:
      - checkout
      - run:
            ls .
      - run:
          name: validate ami.json
          command:
	    packer validate ami.json
      - run:
          name: build ami
          command: packer build -var-file=vars.json ami.json
