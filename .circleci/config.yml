version: 2
jobs:
  build:
    docker:
      - image: hashicorp/packer:1.1.1

    steps:
      - checkout
      - run:
          name: validate ami.json
          working_directory: ~/project
          command: packer validate ami.json
      - run:
          name: build ami
          working_directory: ~/project
          command: packer build -var 'aws_access_key=AKIAYKX5CVIVAMFQADST' -var 'aws_secret_key=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' -var 'aws_region=us-east-1' ami.json
